.row
  .span10.offset1
    .jumbotron
      %h1 Työmääräykset
      %p 
        Tästä näet kaikki luodut työmääräykset ja niiden nykyiset tilat. 
        Allaolevasta napista voit luoda uuden työmääräyksen. 
        Luomallesi työmääräykselle annetaan automaattisesti uniikki työnumero, 
        jonka avulla voit aina viitata ristiriidattomasti samaan työhön.
      %p= link_to "Luo työmääräys", new_order_path, :class => "btn btn-primary btn-lg", role: "button"
%table.table.table-striped
  %thead
    %tr
      %th ID 
      %th Kohde
      %th Kuvaus
      %th Tila
      %th Toiminnot
  %tbody
    - @orders.each do |order|
      %tr
        %td
          %span{class: "label label-#{status_to_class(order.status)}"}= order.id
        %td= link_to order.title, order
        %td= order.description
        %td= I18n.t order.status
        %td
          = link_to "Muokkaa", edit_order_path(order), :class => "btn btn-mini btn-primary"
          = link_to "Poista", order_path(order), method: :delete, confirm: "Poistetaanko #{order.title}?", :class => "btn btn-mini btn-danger"
