%table.table.table-striped
  %thead
    %tr
      %th Numero
      %th Kohde
      %th Kuvaus
  %tbody
    - @orders.each do |order|
      %tr
        %td= link_to "Työnumero", order_path(order)
        %td= order.title
        %td= order.description
